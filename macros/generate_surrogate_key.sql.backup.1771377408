{%- macro generate_surrogate_key(column_list) -%}
    {%- if execute -%}
        {%- set col_str = ", ".join(["CAST(" + col + " AS STRING)" for col in column_list]) -%}
        TO_HEX(MD5(CONCAT({{ col_str }})))
    {%- else -%}
        null
    {%- endif -%}
{%- endmacro -%}