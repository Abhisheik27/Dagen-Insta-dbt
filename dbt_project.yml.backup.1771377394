analysis:
  +schema: analysis
analysis-paths:
- analysis
clean-targets:
- target
- dbt_packages
config-version: 2
data-paths:
- data
macro-paths:
- macros
model-paths:
- models
models:
  Dagen_Insta_dbt:
    dimensions:
      +materialized: table
      +schema: dimensions
    facts:
      +materialized: table
      +schema: facts
    intermediate:
      +materialized: view
      +schema: intermediate
    reporting:
      +materialized: view
      +schema: reporting
    staging:
      +materialized: view
      +schema: staging
name: Dagen_Insta_dbt
profile: Dagen-Insta-dbt
seeds:
  Dagen_Insta_dbt:
    +schema: seeds
snapshot-paths:
- snapshots
target-path: target
test-paths:
- tests
tests:
  +schema: tests
version: 1.0.0
