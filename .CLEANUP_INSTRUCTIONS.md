# Phase 1 Cleanup Instructions

## Files to Delete (Safe to Remove)

### 1. Target Directory
```bash
rm -rf target/
```
- **Size:** 2-5MB
- **Contains:** Compiled SQL, artifacts, manifest.json, run_results.json
- **Safe to delete:** YES - will be regenerated on next `dbt run` or `dbt compile`

### 2. Backup Files (7 total)
```bash
rm -f MODELS_SUMMARY.md.backup.1776146207
rm -f models/schema_3nf_normalized.yml.backup.1776146207
rm -f models/staging/stg_insta_posts.sql.backup.1776146778
rm -f models/staging/stg_posts.sql.backup.1776146788
rm -f models/staging/stg_insta_posts_clean.sql.backup.1776146777
rm -f models/bridges/bridge_post_mentions.sql.backup.1776146216
rm -f macros/generate_surrogate_key.sql.backup.1771377408
```
- **Size:** ~30KB combined
- **Purpose:** Temporary backups from file restoration
- **Safe to delete:** YES - originals are safe

### 3. Empty/Unused Schema File
```bash
rm -f models/schema_3nf_normalized.yml
```
- **Size:** 0 bytes (empty)
- **Purpose:** Caused validation warnings, not referenced
- **Safe to delete:** YES - use schema_3nf.yml instead if needed

## Total Space Reclaimed
- **Files deleted:** ~125 files
- **Space freed:** ~3-5MB

## How to Run Cleanup

### Option 1: Manual Git Removal
```bash
cd /tmp/workspace_82/dbt/Dagen-Insta-dbt
rm -rf target/
find . -name "*.backup*" -delete
rm -f models/schema_3nf_normalized.yml
git add -A
git commit -m "cleanup: phase 1 - remove generated artifacts and backups"
git push
```

### Option 2: Run the Script
```bash
bash .cleanup_phase1.sh
git add -A
git commit -m "cleanup: phase 1 - remove generated artifacts and backups"
git push
```

## Verification
After cleanup, verify:
```bash
# Check git status is clean
git status

# Verify target/ is gone
ls -la target/  # Should show "No such file or directory"

# Verify no .backup files exist
find . -name "*.backup*" | wc -l  # Should return 0

# Verify pipeline still compiles
dbt compile
dbt run
```

## ⚠️ Important Notes
- This cleanup is **completely safe** - all deleted files are generated or temporary
- The pipeline will work normally after cleanup
- Next `dbt run` will regenerate the target/ directory
- All model definitions and tests are preserved
- Version control history is preserved

---
**Phase 1 Cleanup Status:** Ready to execute
**Date:** April 14, 2026